interface:
  display_name: "RIK Mint"
  short_description: "Mint fcf Repository Identity Keys for GitHub repositories"
  default_prompt: "Use the RIK mint skill to set up fcf CLI registration, link the repository wallet without exposing private keys, configure GitHub Actions variables, dispatch the registration workflow, and report the transaction hash."
