interface:
  display_name: "RIK Mint"
  short_description: "Mint fcf Repository Identity Keys for GitHub repositories"
  default_prompt: "Use $rik-mint to register a GitHub repository against a confirmed RIK deployment, safely link its wallet, run the OIDC workflow, and verify the mint."
